import 'package:flutter/material.dart';

class RandomItem extends StatelessWidget {
  const RandomItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('❤️ Random', style: TextStyle(fontSize: 24)),
    );
  }
}
