import 'package:flutter/material.dart';

class ChooserItem extends StatelessWidget {
  const ChooserItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('🏠 Chooser', style: TextStyle(fontSize: 24)),
    );
  }
}
