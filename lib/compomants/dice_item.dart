import 'package:flutter/material.dart';

class DiceItem extends StatelessWidget {
  const DiceItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('👤 Dice', style: TextStyle(fontSize: 24)));
  }
}
