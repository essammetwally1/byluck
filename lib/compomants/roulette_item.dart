import 'package:flutter/material.dart';

class RouletteItem extends StatelessWidget {
  const RouletteItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('➕ Roulette', style: TextStyle(fontSize: 24)),
    );
  }
}
