import 'package:flutter/material.dart';

class ArrowItem extends StatelessWidget {
  const ArrowItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('🔍 Arrow', style: TextStyle(fontSize: 24)),
    );
  }
}
